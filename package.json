{
  "name": "handitoff",
  "version": "0.0.0",
  "description": "Browser-based file handoff app for moving files between devices.",
  "private": true,
  "license": "MIT",
  "type": "module",
  "homepage": "https://handitoff.io",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/handitoff/handitoff.git"
  },
  "bugs": {
    "url": "https://github.com/handitoff/handitoff/issues"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build:apps",
    "build:packages": "npm run build -w @handitoff/protocol && npm run build -w @handitoff/config && npm run build -w @handitoff/crypto && npm run build -w @handitoff/transfer && npm run build -w @handitoff/turn && npm run build -w @handitoff/abuse && npm run build -w @handitoff/analytics",
    "build:apps": "npm run build -w @handitoff/api && npm run build -w @handitoff/web",
    "build:api": "npm run build:packages && npm run build -w @handitoff/api",
    "build:web": "npm run build:packages && npm run build -w @handitoff/web",
    "typecheck": "npm run build:packages && npm run typecheck:apps",
    "typecheck:apps": "npm run typecheck -w @handitoff/api && npm run typecheck -w @handitoff/web",
    "lint": "eslint .",
    "test": "npm run test --workspaces --if-present",
    "prisma:generate": "npm run prisma:generate -w @handitoff/api",
    "prisma:migrate:deploy": "npm run prisma:migrate:deploy -w @handitoff/api",
    "prisma:migrate:dev": "npm run prisma:migrate:dev -w @handitoff/api",
    "prisma:db:push": "npm run prisma:db:push -w @handitoff/api",
    "prisma:migrate:reset": "npm run prisma:migrate:reset -w @handitoff/api",
    "format": "prettier --check \"**/*.{ts,tsx,js,jsx,mjs,json,md,css,yml}\"",
    "dev": "concurrently \"npm run dev:web\" \"npm run dev:api\" --names \"web,api\" --prefix-colors \"bgBlue.bold,bgGreen.bold\"",
    "dev:lan": "node scripts/dev-lan.mjs",
    "dev:web": "npm run dev -w @handitoff/web -- --host 0.0.0.0",
    "dev:api": "npm run dev -w @handitoff/api",
    "start:api": "npm run build -w @handitoff/api && npm run start -w @handitoff/api",
    "start": "node apps/api/dist/server.js"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "@types/node": "^22.19.19",
    "concurrently": "^8.2.0",
    "eslint": "^9.25.1",
    "prettier": "^3.5.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.31.0",
    "vitest": "^3.1.2"
  },
  "dependencies": {
    "qrcode.react": "^4.2.0"
  }
}
